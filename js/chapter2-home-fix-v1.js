(()=>{'use strict';const app=document.getElementById('app');if(!app)return;function isChapter2Home(){const shell=document.querySelector('.c2-shell');if(!shell)return false;const h1=shell.querySelector('h1');return !!h1&&/дом родителей/i.test(h1.textContent||'')}function markMalformedBlocks(root){[...root.querySelectorAll('section,div,aside,article')].forEach(node=>{const text=(node.textContent||'').replace(/\s+/g,' ').trim();if(!text)return;if(text.includes('Если точка на сцене не нажимается')||text.includes('Отметить обязательные точки')||text==='поверх атмосферной сцены.'||text.endsWith('поверх атмосферной сцены.'))node.classList.add('c2hf-hide-old')})}function findActionButton(root,labelPart){return [...root.querySelectorAll('button,[role="button"]')].find(btn=>(btn.textContent||'').toLowerCase().includes(labelPart.toLowerCase()))||null}function ensureHelper(root){const footer=root.querySelector('.c2-footer');if(!footer)return;if(footer.querySelector('.c2hf-helper-shell'))return;const shell=document.createElement('div');shell.className='c2hf-helper-shell';const card=document.createElement('section');card.className='c2hf-helper';card.innerHTML='<b>Если точка на сцене не нажимается</b><p>Можно отметить обязательные осмотры крупной кнопкой, без охоты за пикселями на картинке.</p>';const action=findActionButton(root,'отметить обязательные');const btn=document.createElement('button');btn.type='button';btn.className='c2hf-helper-btn';btn.textContent='Отметить обязательные точки';btn.addEventListener('click',()=>{if(action&&!action.disabled){action.click()}else{root.querySelectorAll('.c2-check:not(.done), [data-chapter2-fallback-key]:not(.done)').forEach(node=>node.click?.())}});card.appendChild(btn);shell.appendChild(card);footer.prepend(shell)}function normalizePrimaryButton(root){const footer=root.querySelector('.c2-footer');if(!footer)return;const buttons=[...footer.querySelectorAll('button')];const primary=buttons.find(btn=>/отправиться к соседнему дому/i.test(btn.textContent||'')||btn.classList.contains('primary-button'));if(primary)primary.classList.add('c2hf-next-button')}let queued=false;function apply(){const active=isChapter2Home();document.body.classList.toggle('c2-home-fix-active',active);if(!active)return;const shell=document.querySelector('.c2-shell');if(!shell)return;markMalformedBlocks(shell);ensureHelper(shell);normalizePrimaryButton(shell)}function schedule(){if(queued)return;queued=true;requestAnimationFrame(()=>{queued=false;apply()})}if(document.readyState==='loading'){document.addEventListener('DOMContentLoaded',schedule,{once:true})}else{schedule()}new MutationObserver(schedule).observe(app,{childList:true,subtree:true})})();
